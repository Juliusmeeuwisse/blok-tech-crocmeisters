<section class="profile">
  <article>
    <img src='{{profileImg}}' alt="profiel foto">
    <ul>
      <li>Username:</li>
      <li>{{spotifyProfile.display_name}}</li>
      <li>E-mail:</li>
      <li>{{spotifyProfile.email}}</li>
      <li>Profile:</li>
      <li><a href="{{spotifyProfile.external_urls.spotify}}">{{spotifyProfile.external_urls.spotify}}</a></li>
    </ul>
    <a href="https://accounts.spotify.com/en/logout">Logout</a>
  </article>
</section>
<section class="musicList">
  <h1 id="profileH1">My Favourite Songs:</h1>
  {{#each myProfile.songs}}
  <article>
    <img src='{{albumArt}}' alt="favourite song 1">
    <div>
      <audio src="{{source}}"></audio>
      <button class='play'><span></span></button>
      <h2>{{title}}</h2>
      <p>{{artist}}</p>
    </div>
  </article>

  {{/each}}


 <button id="changeSongs">Change Songs</button>
 
  <form action="toevoegen" method="post" class="editform">
    <section class="change">
      <h2>Change your favourite songs</h2>
      {{#each myProfile.songs}}
      <ul>
        <li>
          <input id="song" type="text" name="Song" placeholder="Title" required>
        </li>
        <li>
          <input id="song" type="text" name="lastname" placeholder="Artist" required>
        </li>
      </ul>
      {{/each}}
       <button id="submitButton">Submit</button>
    </section>
   
  </form>

{{!-- 
  <article>
    <form action="/profile" method="post" class="hiddenForm">
      <input type="text" name="search" placeholder="search for artist or song title" class="hiddenForm">
      <button class="hiddenForm">Search</button>
    </form>
  </article> --}}


  {{#each data}}
  <p>{{this}}</p>
  {{/each}}
</section>
<section>
  <article>
    <h2>My Genres</h2>
    {{#each myProfile.genres}}
    <ul>
      <li>{{this}}</li>
    </ul>
    {{/each}}
  </article>
</section>